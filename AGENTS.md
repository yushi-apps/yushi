# Repository Guidelines

## 项目结构和模块组织

- 子Agent：`agents/`，存储子Agent的Markdown描述文件
- 文档：`docs/`
- CLI：`app\`
- Agent引擎：`jieyusha/`
- 可逆计算引擎：`tuo`，定义文件在`tuo/xdefs/`，其中`xdsl.xdef`是差量合并操作定义文件，`memory.xdef`是LLM记忆定义文件，`task.xdef`是任务定义文件。
- 工具：`tools`，
- 技能：`skills`

## 编码规则

- 语言：Rust，严格遵循官方规范。
- 不要为你未更改的代码添加文档字符串、注释或类型注解。仅在逻辑不显而易见的地方添加简单注释。
- 不要添加超出要求的新功能、重构代码，或进行所谓的"改进"，修复一个 bug 不需要顺手清理周边代码。
- 一个简单功能不需要额外的可配置性。
- 不要为不可能发生的场景添加错误处理、回退机制或验证逻辑。应信任内部代码和框架本身的保证。仅在系统边界（用户输入、外部 API）处进行验证。
- 当你可以直接修改代码时，不要使用功能开关或向后兼容的填充代码。
- 不要为一次性操作创建辅助函数、工具类或抽象层。
- 不要为假设的未来需求进行设计。恰当的复杂度正是任务实际所需的复杂度——既不要推测性的抽象，也不要半成品的实现。三行相似的代码，也好过一次过早的抽象。
- 默认不写注释，只有在"为什么这么做"并不显然时才写，例如存在隐藏约束、微妙不变量、某个特定 bug 的规避方案。如果删除该注释不会让未来的代码阅读者感到困惑，那就不要写它。
- 不要解释代码"做了什么"，因为命名良好的标识符已经能够说明这一点。不要在代码里引用当前任务、修复内容或调用方（例如"被 X 使用"、“为 Y 流程添加”、"处理 issue#123 中的情况"），因为这些内容应写在 PR 描述里，并且在代码库演进过程中会逐渐失效。
- 不要删除已有的注释，除非你正在移除它们所描述的代码，或者你确定这些注释是错误的。一个在你看来毫无意义的注释，可能编码了一个在当前代码变更中不可见的约束条件或过去某个 bug 带来的教训。
- 在报告任务完成之前，请验证它确实有效：运行测试、执行脚本、检查输出。最低复杂度意味着不要画蛇添足，而不是跳过终点线。如果你无法验证（例如没有测试、无法运行代码），请明确说明，而不是声称成功。
- 一般来说，不要对你尚未阅读过的代码提出修改建议。如果用户询问或希望您修改某个文件，请先阅读它。在提出修改建议之前，先理解现有的代码。
- 如果你注意到用户的需求基于一个误解，或者发现了与他们所提问题相邻的 bug，请直接指出来。你是一位协作者，而不仅仅是一个执行者——用户受益于你的判断力，而不仅仅是你的服从。
- 除非对于实现你的目标绝对必要，否则不要创建文件。通常，编辑现有文件比创建新文件更可取，因为这可以防止文件臃肿，并更有效地基于现有工作进行构建。
- 如果一个方法失败了，在切换策略之前要先诊断原因——阅读错误信息，检查你的假设，尝试进行有针对性的修复。不要盲目地重试完全相同的操作，但也不要因为一次失败就放弃一个可行的方案。只有在经过调查后确实遇到无法解决的困难时，才向用户求助，而不是一遇到阻力就立刻寻求帮助。
- 务必谨慎，避免引入安全漏洞，例如命令注入、跨站脚本攻击（XSS）、SQL注入以及其他OWASP十大安全漏洞。如果你发现自己编写了不安全的代码，请立即修复。优先编写安全、可靠且正确的代码。
- 避免使用向后兼容的临时方案，例如重命名未使用的 `_vars` 变量、重新导出类型、为已删除的代码添加 `// removed` 注释等。如果你确信某些代码未被使用，可以直接将其完全删除。
- 命名规范：产品、应用及文档标题使用`Yushi`；CLI 命令、包名/二进制文件、路径及配置键名则使用`yushi`。